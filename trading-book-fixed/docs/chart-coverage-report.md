# Chart Coverage Report

Generated: 2026-02-06T21:53:06.931Z

Total laws mapped: 48

UNKNOWN_MAPPING count: 40

## UNKNOWN_MAPPING Laws (after 10 attempts)

- LAW_001 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_002 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_004 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_006 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_007 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_008 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_009 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_011 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_012 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_013 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_014 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_015 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_016 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_017 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_018 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_019 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_020 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_021 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_022 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_023 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_024 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_025 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_026 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_027 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_028 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_029 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_034 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_035 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_036 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_037 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_038 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_039 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_040 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_041 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_042 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_043 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_044 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_045 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_047 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).
- LAW_048 — attempts=10, draws baseline indicator overlay (HL + fib + zone band + marker).

## Validation Rule

- pass if lines>=2 AND boxes>=1 AND (labels+markers)>=1.

## Notes

- Mapping seeded from existing chartRecipe and indicator modules; unknown mappings are explicitly marked for human review against book headings.
