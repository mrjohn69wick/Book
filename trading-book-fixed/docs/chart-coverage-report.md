# Chart Coverage Report

Generated: 2026-02-06T20:59:07.624Z

Total laws mapped: 48

UNKNOWN_MAPPING count: 40

## UNKNOWN_MAPPING Laws

- LAW_001 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_002 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_004 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_006 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_007 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_008 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_009 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_011 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_012 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_013 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_014 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_015 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_016 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_017 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_018 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_019 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_020 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_021 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_022 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_023 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_024 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_025 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_026 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_027 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_028 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_029 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_034 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_035 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_036 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_037 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_038 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_039 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_040 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_041 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_042 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_043 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_044 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_045 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_047 — fallback draws unit HL + fib grid + zone band + marker.
- LAW_048 — fallback draws unit HL + fib grid + zone band + marker.

## Notes

- Mapping seeded from existing `chartRecipe` plus indicator-driven neutral fallback.
- Review canonical sections in both books for future hard mappings.
